%% name = NodePattern::Parser

%% {
  attr_reader :ast
}

spaces = /[\s]*/
nodetype = /[a-zA-Z][a-zA-Z0-9_-]*/:t { t }

root = spaces start spaces

start = &. statement:c { @ast = c  }

statement = node

node = '(' spaces < nodetype > spaces ')' {
  NodePattern::Node.new(text, [])
}
